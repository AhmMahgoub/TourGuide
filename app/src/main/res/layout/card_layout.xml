<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    card_view:cardBackgroundColor="@color/ListItemColor"
    card_view:cardCornerRadius="@dimen/dim12"
    card_view:cardElevation="@dimen/dim3"
    card_view:contentPadding="@dimen/dim4"
    android:foreground="?selectableItemBackground"
    style="@style/parentlayoutCardView"
     >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/dim16" >

        <TextView
            android:id="@+id/item_title"
            style="@style/TitleOfCardView"
            />

        <TextView
            android:id="@+id/item_detail"
            style="@style/subjectOfCardView"
            />
    </LinearLayout>
</android.support.v7.widget.CardView>